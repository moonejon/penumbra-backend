services:
  postgres:
    image: 'postgres:18'
    environment:
      - 'POSTGRES_DB=${DB_NAME}'
      - 'POSTGRES_PASSWORD=${DB_PASSWORD}'
      - 'POSTGRES_USER=${DB_USERNAME}'
    ports:
      - '5432:5432'
    volumes:
      - 'penumbra_pgdata:/var/lib/postgresql'

volumes:
  penumbra_pgdata: